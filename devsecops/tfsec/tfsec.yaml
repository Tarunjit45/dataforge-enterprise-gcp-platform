# =============================================================================
# tfsec Infrastructure as Code (IaC) Security Scanner Configuration
# =============================================================================

minimum_severity: HIGH

exclude:
  - google-storage-bucket-encryption-customer-key # Managed via default Google CMEK fallback when KMS not specified

soft_fail: true
