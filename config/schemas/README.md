# BigQuery & Ingestion JSON Schemas

This directory holds JSON-formatted schema contract definitions for ingestion payload validation and BigQuery table creation.

## Structure
* `bq_schemas/`: BigQuery table schema definitions (e.g. `dim_customers.json`, `fact_sales.json`).
* `ingestion_schemas/`: Source payload API contracts for header and field type validation.
